[package]
name = "agent-consensus"
version = "0.1.0"
edition = "2024"
description = "Build consensus from multiple agent responses"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
clap = { version = "4", features = ["derive"] }
